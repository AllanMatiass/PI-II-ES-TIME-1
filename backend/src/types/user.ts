export interface UserLoginDTO {
    email: string;
    password: string;
}

export interface UserCadDTO {
    name: string;
    phone: string;
    email: string;
    password: string;
    confirmPassword: string;
}