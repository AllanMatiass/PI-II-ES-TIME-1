export interface UserLoginDTO {
    email: string;
    senha: string;
}

export interface UserCadDTO {
    nome: string;
    telefone: string;
    email: string;
    senha: string;
    confirmaSenha: string;
}